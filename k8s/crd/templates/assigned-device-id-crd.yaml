{
  "apiVersion": "apiextensions.k8s.io/v1",
  "kind": "CustomResourceDefinition",
  "metadata": {
    "name": "assigneddeviceids.injector.sdp.com"
  },
  "spec": {
    "group": "injector.sdp.com",
    "names": {
      "categories": [],
      "kind": "AssignedDeviceId",
      "plural": "assigneddeviceids",
      "shortNames": [],
      "singular": "assigneddeviceid"
    },
    "scope": "Namespaced",
    "versions": [
      {
        "additionalPrinterColumns": [],
        "name": "v1",
        "schema": {
          "openAPIV3Schema": {
            "description": "Auto-generated derived type for AssignedDeviceIdSpec via `CustomResource`",
            "properties": {
              "spec": {
                "description": "AssignedDeviceId CRD This is the CRD where we device ids already assigned to some PODs Spec for AssginedDeviceId CRD This CRD defines an already assigned device id and the distinguished name used by it",
                "properties": {
                  "device_id": {
                    "type": "string"
                  },
                  "distinguished_name": {
                    "type": "string"
                  }
                },
                "required": [
                  "device_id",
                  "distinguished_name"
                ],
                "type": "object"
              }
            },
            "required": [
              "spec"
            ],
            "title": "AssignedDeviceId",
            "type": "object"
          }
        },
        "served": true,
        "storage": true,
        "subresources": {}
      }
    ]
  }
}
